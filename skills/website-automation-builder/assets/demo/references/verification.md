# Fixture verification

Run typecheck, build, unit/process tests and the fixture-server smoke check through the builder's scripts/test-demo.mjs. That runner writes exact test evidence and build-state after success. A freshly generated demo is not yet verified. Real extension/CDP evidence must be recorded separately; fake transport does not demonstrate a live browser.
