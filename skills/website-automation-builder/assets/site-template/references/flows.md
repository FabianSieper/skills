# Flows

BUILD_REQUIRED: Record starting state, identity, business steps, expected state and recovery action for each flow. Document alternatives confirmed by the user. After two targeted failed navigation attempts, ask for the real route; block only that action.
