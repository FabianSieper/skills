# TODOs
This file contains todo-prompts which are to be executed in the future

1. Do not use waitfortimeout anywhere, but instead wait for specific events, like appearing ui elements.
2. I want not only the documentation to be state-driven, but also the code itself: Each POM is a state in the browser / a page in the browser. Transitions betwen Stages can happen either within a POM or by transforming a pom into another, like by navigating to a different page. Each transition between stages is done via pom methods. The stages between documentation and code should match. Maybe this whole idea is already implemented. If so, ignore, if not, apply
3. Implement new state: The offer page of the user themselves. how to get there: Click on "selling" -> "my offers" -> "singles". that page has filter on the left hand side which are required to be editable by the agent, but especially the card name filter, which will be the most useful. also, you can get to the next page, containing the next few hits. if asked to list all offers or something, the agent should go through all pages until the last one is reached using the navigation buttons at the bottom of the table. if searching for a card, the cardname filter should be used. YOu can also get to the details page of a card including its types by clicking on the name of a card in thie listing here. that allows to chceck the set price of the user compared to other prices of other offers. make sure, when reading the other offers, that the filter matches the default-filter or the requested filter by the user.
4. The website-automation-builder should describe the target architecture and progress. the cardmarket automation was build without the target architecture of website automation builder in mind. the webseite automation builder and the architecture and how it works in generell of the cardmarket automation should match. for that, identify points in which the both differ, think which approach is smarter and either adjust the cardmarket-automation or the website-automation-builder in that way.