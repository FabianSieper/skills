# Fixture flows

inventory.find: inventory state → SKU field → search → query-specific result. inventory.update-title: visible exact item → pure preview → approved edit → title → save → title/version assertion. Unknown state stops the domain flow; global observe remains available. These flows describe fixtures/inventory.html only.
