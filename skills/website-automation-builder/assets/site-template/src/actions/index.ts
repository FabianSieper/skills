import type { Action } from '../runtime/engine.ts';
// BUILD_REQUIRED: Import and register the verified website actions here.
// Never register the fixture/demo actions as if they belonged to the real website.
export const actions: readonly Action[] = [];
